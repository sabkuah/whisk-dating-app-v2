import { Typography } from '@material-ui/core';

const ConfirmAccount = () => {
  return (
    <div>
      <Typography variant="h5" style={{textAlign: "center", marginTop: "3em"}}>
        Please confirm your account before starting
      </Typography>
    </div>
  );
};

export default ConfirmAccount;
