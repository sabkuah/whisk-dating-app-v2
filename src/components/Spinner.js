import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

//NEED TO STYLE THIS --> CENTER, BRAND COLOUR

const Spinner = () => {
  return (
    <div>
      <CircularProgress />
    </div>
  );
};

export default Spinner;
